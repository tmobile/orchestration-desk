{
  "name": "togglerefresher",
  "version": "1.0.0",
  "description": "Makes refresh config calls to spring boot config clients to refresh the configs.",
  "main": "index.ts",
  "keywords": [
    "Toggles",
    "Refresh",
    "SpringBoot",
    "Client"
  ],
  "scripts": {
    "test": "jest",
    "build": "tsc",
    "prepare": "yarn run build",
    "prepublishOnly": "yarn test && yarn lint",
    "format": "prettier --write \"src/**/*.ts\" \"src/**/*.js\"",
    "lint": "tslint -p tsconfig.json",
    "preversion": "npm run lint"
  },
  "author": "Parth Patel",
  "license": "MIT",
  "dependencies": {
    "@types/es6-promise": "^3.3.0",
    "@types/request": "^2.47.1",
    "es6-symbol": "^3.1.1",
    "request": "^2.87.0",
    "typescript": "^2.9.1"
  },
  "devDependencies": {
    "@types/jasmine": "^2.8.8",
    "@types/jest": "^23.3.1",
    "@types/node": "^10.5.4",
    "jest": "^23.4.1",
    "prettier": "^1.14.0",
    "ts-jest": "^23.0.1",
    "tslint": "^5.11.0",
    "tslint-config-prettier": "^1.14.0"
  }
}
